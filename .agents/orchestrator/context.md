# Context: OBJ-03 Tasks 3 & 4

## Objective
Implement non-destructive ECC script integration, update installer to use merge algorithm, expand verification checks, execute installation pass, and create checkpoint for OBJ-03.

## Environment & Rules
- Workspace Root: `d:/dev/agy-os`
- Target Repo: `d:/CLAUDE-PROJECT/website` (READ-ONLY)
- All shell executions MUST use Git Bash (`bash`).
- Path formatting MUST use forward slashes (`/`).

## Task Details
- Task 3.1-3.5: `harness/agy-script/scripts/merge-hooks-agy.js` and `install-apply-agy.js`.
- Task 4.1: `harness/agy-script/scripts/verify-installation-agy.js`.
- Task 3.6, 4.2-4.5: Execution, validation, task.md updates, and `/checkpoint create "OBJ-03-Complete"`.
